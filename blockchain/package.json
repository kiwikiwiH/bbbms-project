{
  "name": "blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "BBBMS smart contracts (Hardhat 3)",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "hardhat": "^3.9.0",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "ethers": "^6.14.0"
  }
}
