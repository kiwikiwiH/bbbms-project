# BBBMS Blockchain

Hardhat 3 project for the `BloodBank` smart contract.

## Commands

```bash
npm install
npm run compile
npm run deploy
```

Deploy requires Ganache running at `http://127.0.0.1:7545`.

After deploy, copy the contract address into `backend/.env`.
