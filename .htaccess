<IfModule mod_rewrite.c>
    RewriteEngine On

    # Laragon / XAMPP often serve the repo root (bbbms-project/).
    # Forward every request into the Laravel public directory.
    RewriteCond %{REQUEST_URI} !^/tarrlok/public/
    RewriteRule ^(.*)$ tarrlok/public/$1 [L]
</IfModule>
